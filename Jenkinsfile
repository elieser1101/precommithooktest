#!groovy
node {
    git url: 'https://github.com/elieser1101/precommithooktest', poll: false
    sh "pwd & ls"
    sh "env && echo prueba2pullrequest1"
    sh "echo $payload"
    sh "echo ${payload}"
}
